# 版本记录（Version Log）

> 留存机制：每个版本同时以 **git tag** 和 **docs/ 下的版本文档** 双重留存，便于回溯与回滚。
> 查看标签：`git tag -n`；切到某版本：`git checkout v0.3.0`。

## v0.1.0 —— MVP（2026-09-03）

- **Git 标签**：`v0.1.0`
- **代表性提交**：`4dbe104`（docs: 新增 MVP 源码讲解）
- **实现内容**：
  - MVP 全链路：采集（synthetic / pcap 回放 / Linux live 接口）→ 解析 → 流表 → 聚合 → 存储（JSONL file）→ REST API → 内嵌仪表盘；
  - pcap 回放集成测试、8 个 Go 包单元测试全绿。
- **留存文档**：产品设计文档、技术方案、系统设计文档、源码讲解。

## v0.2.0 —— SQLite + 环境迁移 + Linux 验收（2026-09-04）

- **Git 标签**：`v0.2.0`
- **代表性提交**：`ef005b8`（docs: 补充 CentOS 部署记录与文档索引，含本文件）
- **实现内容**：
  - 存储后端接入 **SQLite**（纯 Go 驱动 modernc.org/sqlite，无 CGO）；FileStore 保留可切换；
  - Go 工具链与全部缓存迁至 D 盘（GOROOT=D:\Go，GOPATH/模块缓存=D:\31387\APPs\go）；
  - **Linux 连通验收**：WSL Ubuntu 与 CentOS 虚拟机（192.168.161.128，用户 wuki）真实抓包运行成功，Windows 浏览器可访问仪表盘；
  - README 整合模块总览与版本路线；新增《使用指南》。
- **留存文档**：V0.2-运行原理与SQLite接入.md、使用指南.md、版本记录.md。

## v0.3.0 —— 远程桌面客户端 V1（2026-09-05）

- **Git 标签**：`v0.3.0`
- **代表性提交**：待填写（本次改动最后一次提交后补充）
- **实现内容**：
  - **后端增强**：新增可选 Bearer Token 鉴权（`[api] token`，空则不鉴权），`/api/` 前缀请求校验 `Authorization: Bearer <token>`，静态页面不鉴权；含鉴权测试；
  - **Windows 桌面客户端（Tauri 2 + Vue 3 + Vite + ECharts，目录 `client/`）**：
    - 连接管理：保存多组"服务器地址 + 令牌"配置，本地持久化（localStorage）；
    - 仪表盘：实时带宽 / 包速率 / 并发连接 / 丢包率 KPI（1s 轮询）、最近 1 小时历史曲线（ECharts）、会话明细（IP/协议过滤 + 分页）、系统健康信息；
    - 请求统一走 Rust 命令（reqwest）访问远程 API，令牌保存在进程状态中，规避 CORS；
  - **前端测试**：纯函数单元测试（node --test）覆盖地址校验、速率/字节/包速率格式化；
  - 使用方式：`cd client && npm install && npx tauri build`，产物在 `client/src-tauri/target/release/bundle/`。
- **留存文档**：README、产品设计文档、技术方案、使用指南、版本记录。

## 后续版本（规划）

| 版本 | 内容 | 状态 |
| --- | --- | --- |
| V0.3（V1 客户端） | Windows Tauri 桌面客户端、令牌鉴权 | 已完成（2026-09-05） |
| V2 | 告警引擎、TOP N、协议分布、IPv6、多队列、WebSocket 实时、界面插件框架 | 规划中 |
| V3 | 客户端增强：实时推送接入、多机对比、Windows 通知、帮助中心型 AI | 规划中 |
| V4 | 流量控制：Linux 端 tc 限速（仅本机流量，默认关闭 + 二次确认）+ 控制 API/界面 | 规划中 |
| V5 | LLM 助手（RAG）、Agent+Server 分布式、NetFlow/sFlow、DPI | 规划中 |

> 每完成一个版本：更新本文件 → 提交 → 打 `vX.Y.Z` 标签 → 推送标签。