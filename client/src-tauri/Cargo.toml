[package]
name = "metd"
version = "0.4.0"
description = "Linux 网络流量监控 Windows 客户端"
edition = "2021"

[lib]
name = "metd_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde_json = "1"
