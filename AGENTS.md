# AGENTS.md

本文件面向在本仓库中工作的 AI 代理与开发者，定义项目背景与强制工作流。

## 项目简介

- **项目**：Linux 网络流量监控系统
- **当前阶段**：架构设计阶段，已产出《Linux 网络流量监控系统设计文档》
- **核心文档**：
  - [docs/网络流量监控系统设计文档.md](docs/网络流量监控系统设计文档.md)
  - [docs/Linux网络流量监控系统设计文档.docx](docs/Linux网络流量监控系统设计文档.docx)
- **技术方向**：Go 语言，AF_PACKET/libpcap 采集，分层模块化架构（采集 → 解析 → 聚合 → 存储 → API → 展示 → 告警）

## 强制工作流规则

以下规则适用于本仓库中的每一次改动，任何代理都必须遵守：

1. **每次改动必须提交**：任何改动完成后，必须创建一个对应的 Git commit，便于后续追踪和回滚。
2. **每次改动必须带测试**：改动后必须编写或更新相关测试；交付给用户前，必须确保所有测试与验证全部通过。
3. **提交信息规范**：使用 Conventional Commits 格式 `type(scope): subject`，例如：
   - `docs: 新增架构设计文档`
   - `feat(parser): 支持 IPv6 解析`
   - `test(storage): 补充流记录写入测试`
4. **不提交生成物**：编译产物、日志、临时文件不得入库；相关路径应写入 `.gitignore`。
5. **工作目录限定**：所有与本项目相关的操作（文件创建与修改、测试、git 提交与推送等）一律在本仓库根目录内执行，不得在仓库之外生成或修改项目文件；当前仓库根目录为 `D:\Codex\Linux network control`。

## 验证流程

提交前按改动类型执行对应验证，全部通过后才允许交付：

| 改动类型 | 验证命令 / 检查项 |
| --- | --- |
| 文档改动 | 检查内容完整、结构正确、链接有效 |
| Go 代码 | `go build ./...`、`go vet ./...`、`go test ./...` |
| 配置改动 | 配置解析校验（加载配置并打印） |
| 提交前检查 | `git status`（无遗漏）、`git diff`（改动符合预期） |

## 仓库约定

- 交流语言：与用户沟通使用中文；代码注释使用中文。
- 提交粒度：小步提交，一次 commit 只包含一个逻辑改动。
- 分支策略：当前使用单一主干分支（main），功能开发以模块为单元推进。
- 文档同步：接口、数据结构等变更必须同步更新设计文档。
